<!DOCTYPE html>
<html lang="en">
<head><%- include('partials/head') %></head>
<body class="min-h-screen bg-shark-950 text-white">

  <!-- Top bar -->
  <header class="sticky top-0 z-10 bg-shark-950/95 backdrop-blur border-b border-white/5 px-6 h-14 flex items-center justify-between">
    <a href="/" class="font-black text-lg text-charm-500 tracking-tight">AllThingsAprons</a>

    <!-- Step indicator -->
    <div class="flex items-center gap-3 text-sm">
      <div class="flex items-center gap-2">
        <div id="step-1-num" class="w-6 h-6 rounded-md bg-charm-600 text-white text-xs font-bold flex items-center justify-center transition-colors">1</div>
        <span id="step-1-label" class="text-white/60 hidden sm:inline transition-colors">Profile</span>
      </div>
      <div class="w-8 h-px bg-white/15"></div>
      <div class="flex items-center gap-2">
        <div id="step-2-num" class="w-6 h-6 rounded-md bg-shark-700 text-white/35 text-xs font-bold flex items-center justify-center transition-colors">2</div>
        <span id="step-2-label" class="text-white/30 hidden sm:inline transition-colors">Contest Entry</span>
      </div>
    </div>

    <div class="w-45"></div>
  </header>

  <div class="flex justify-center px-4 py-10 pb-20">
    <form id="signup-form" action="/signup" method="POST" enctype="multipart/form-data" novalidate class="w-full">

      <!-- ── STEP 1 ── -->
      <div id="step-1-content" class="max-w-xl mx-auto">

        <div class="mb-8">
          <h1 class="text-2xl font-black">Create your profile</h1>
          <p class="text-white/40 mt-1 text-sm">Tell us a bit about yourself to get started.</p>
        </div>

        <!-- Avatar upload -->
        <div class="flex justify-center mb-7">
          <label class="relative cursor-pointer group" title="Upload profile photo">
            <div id="avatar-circle"
              class="w-24 h-24 rounded-md bg-shark-800 border-2 border-dashed border-white/15 flex items-center justify-center overflow-hidden group-hover:border-charm-600/60 transition-colors bg-cover bg-center">
              <div id="avatar-placeholder" class="flex flex-col items-center gap-1 text-white/35 group-hover:text-white/60 transition-colors pointer-events-none">
                <iconify-icon icon="boxicons:camera" width="28"></iconify-icon>
                <span class="text-xs">Photo</span>
              </div>
            </div>
            <div class="absolute -bottom-1.5 -right-1.5 w-6 h-6 rounded-md bg-charm-600 flex items-center justify-center shadow pointer-events-none">
              <iconify-icon icon="boxicons:plus" width="14" class="text-white"></iconify-icon>
            </div>
            <input type="file" name="avatar" id="avatar-input" accept="image/*" class="absolute inset-0 opacity-0 w-full h-full cursor-pointer">
          </label>
        </div>

        <div class="bg-shark-900 border border-white/8 rounded-md p-6 flex flex-col gap-5">

          <!-- Username -->
          <div class="flex flex-col gap-1.5">
            <label for="username" class="text-xs font-semibold text-white/50 uppercase tracking-wide">
              Username <span class="text-charm-500">*</span>
            </label>
            <div class="relative">
              <span class="absolute left-3 top-1/2 -translate-y-1/2 text-white/30 text-sm pointer-events-none">@</span>
              <input id="username" name="username" type="text" placeholder="yourhandle"
                autocomplete="off" spellcheck="false" maxlength="20"
                class="w-full bg-shark-800 border border-white/10 rounded-md pl-7 pr-9 py-2.5 text-sm text-white placeholder:text-white/20 focus:outline-none focus:border-charm-600/50 focus:ring-1 focus:ring-charm-600/25 transition-colors" />
              <span id="username-status" class="absolute right-3 top-1/2 -translate-y-1/2 hidden"></span>
            </div>
            <p id="username-error" class="text-xs text-red-400 hidden"></p>
          </div>

          <!-- Email + Password -->
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-5">
            <div class="flex flex-col gap-1.5">
              <label for="email" class="text-xs font-semibold text-white/50 uppercase tracking-wide">
                Email <span class="text-charm-500">*</span>
              </label>
              <input id="email" name="email" type="email" placeholder="you@email.com" autocomplete="email"
                class="w-full bg-shark-800 border border-white/10 rounded-md px-3 py-2.5 text-sm text-white placeholder:text-white/20 focus:outline-none focus:border-charm-600/50 focus:ring-1 focus:ring-charm-600/25 transition-colors" />
              <p id="email-error" class="text-xs text-red-400 hidden"></p>
            </div>

            <div class="flex flex-col gap-1.5">
              <label for="password" class="text-xs font-semibold text-white/50 uppercase tracking-wide">
                Password <span class="text-charm-500">*</span>
              </label>
              <div class="relative">
                <input id="password" name="password" type="password" placeholder="Min. 8 characters"
                  autocomplete="new-password"
                  class="w-full bg-shark-800 border border-white/10 rounded-md px-3 py-2.5 pr-9 text-sm text-white placeholder:text-white/20 focus:outline-none focus:border-charm-600/50 focus:ring-1 focus:ring-charm-600/25 transition-colors" />
                <button type="button" id="toggle-password"
                  class="absolute right-3 top-1/2 -translate-y-1/2 text-white/30 hover:text-white/60 transition-colors">
                  <iconify-icon id="pw-eye-icon" icon="boxicons:show" width="17"></iconify-icon>
                </button>
              </div>
            </div>
          </div>

          <!-- Confirm Password -->
          <div class="flex flex-col gap-1.5">
            <label for="confirm-password" class="text-xs font-semibold text-white/50 uppercase tracking-wide">
              Confirm Password <span class="text-charm-500">*</span>
            </label>
            <input id="confirm-password" name="confirmPassword" type="password" placeholder="Repeat password"
              autocomplete="new-password"
              class="w-full bg-shark-800 border border-white/10 rounded-md px-3 py-2.5 text-sm text-white placeholder:text-white/20 focus:outline-none focus:border-charm-600/50 focus:ring-1 focus:ring-charm-600/25 transition-colors" />
            <p id="confirm-password-error" class="text-xs text-red-400 hidden"></p>
          </div>

          <!-- Bio -->
          <div class="flex flex-col gap-1.5">
            <label for="bio" class="text-xs font-semibold text-white/50 uppercase tracking-wide">Bio</label>
            <textarea id="bio" name="bio" rows="3"
              placeholder="Tell people about yourself and your passion for aprons..."
              class="w-full bg-shark-800 border border-white/10 rounded-md px-3 py-2.5 text-sm text-white placeholder:text-white/20 focus:outline-none focus:border-charm-600/50 focus:ring-1 focus:ring-charm-600/25 transition-colors resize-none"></textarea>
          </div>

          <!-- Sex + Location -->
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-5">
            <div class="flex flex-col gap-1.5">
              <label for="sex" class="text-xs font-semibold text-white/50 uppercase tracking-wide">
                Sex <span class="text-charm-500">*</span>
              </label>
              <div class="relative">
                <select id="sex" name="sex"
                  class="w-full appearance-none bg-shark-800 border border-white/10 rounded-md px-3 py-2.5 pr-8 text-sm focus:outline-none focus:border-charm-600/50 focus:ring-1 focus:ring-charm-600/25 transition-colors"
                  style="color: rgba(255,255,255,0.22);">
                  <option value="" disabled selected>Select…</option>
                  <option value="male">Male</option>
                  <option value="female">Female</option>
                  <option value="other">Other</option>
                  <option value="prefer-not-to-say">Prefer not to say</option>
                </select>
                <div class="absolute right-3 top-1/2 -translate-y-1/2 pointer-events-none text-white/30">
                  <iconify-icon icon="boxicons:chevron-down" width="16"></iconify-icon>
                </div>
              </div>
              <p id="sex-error" class="text-xs text-red-400 hidden"></p>
            </div>

            <div class="flex flex-col gap-1.5">
              <label for="location" class="text-xs font-semibold text-white/50 uppercase tracking-wide">Location</label>
              <input id="location" name="location" type="text" placeholder="City, Country"
                class="w-full bg-shark-800 border border-white/10 rounded-md px-3 py-2.5 text-sm text-white placeholder:text-white/20 focus:outline-none focus:border-charm-600/50 focus:ring-1 focus:ring-charm-600/25 transition-colors" />
            </div>
          </div>

          <!-- Birthdate -->
          <div class="flex flex-col gap-1.5">
            <label for="birthdate" class="text-xs font-semibold text-white/50 uppercase tracking-wide">
              Date of Birth <span class="text-charm-500">*</span>
            </label>
            <input id="birthdate" name="birthdate" type="date"
              max="<%= new Date(Date.now() - 13*365.25*24*60*60*1000).toISOString().split('T')[0] %>"
              min="1920-01-01"
              style="color-scheme: dark;"
              class="w-full bg-shark-800 border border-white/10 rounded-md px-3 py-2.5 text-sm text-white focus:outline-none focus:border-charm-600/50 focus:ring-1 focus:ring-charm-600/25 transition-colors" />
            <p id="birthdate-error" class="text-xs text-red-400 hidden"></p>
          </div>

          <p id="step1-general-error" class="text-sm text-red-400 hidden"></p>

          <!-- Continue -->
          <button type="button" id="next-btn"
            class="w-full bg-charm-600 hover:bg-charm-500 disabled:opacity-50 disabled:cursor-not-allowed text-white font-bold rounded-md py-3 text-sm transition-colors flex items-center justify-center gap-2">
            Continue
            <iconify-icon icon="boxicons:right-arrow-alt" width="20"></iconify-icon>
          </button>
        </div>
      </div>

      <!-- ── STEP 2 ── -->
      <div id="step-2-content" class="hidden max-w-2xl mx-auto">

        <button type="button" id="back-btn"
          class="flex items-center gap-1.5 text-white/40 hover:text-white transition-colors text-sm mb-7">
          <iconify-icon icon="boxicons:left-arrow-alt" width="18"></iconify-icon>
          Back
        </button>

        <div class="mb-7">
          <h2 class="text-2xl font-black">Enter a live contest</h2>
          <p class="text-white/40 mt-1 text-sm">Pick one contest and submit your apron to complete your signup.</p>
        </div>

        <!-- Hidden contest ID -->
        <input type="hidden" name="contestId" id="contestId" value="">

        <!-- Contest cards -->
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
          <% contests.forEach(c => { %>
          <div class="contest-card cursor-pointer rounded-md overflow-hidden border border-white/10 bg-shark-900 transition-all hover:border-charm-600/40"
               data-contest-id="<%= c._id %>" data-contest-title="<%= c.title %>">
            <div class="aspect-video relative overflow-hidden bg-shark-800">
              <img src="<%= c.coverUrl %>" alt="<%= c.title %>" class="w-full h-full object-cover">
              <div class="contest-check-overlay absolute inset-0 bg-charm-600/50 flex items-center justify-center opacity-0 transition-opacity pointer-events-none">
                <iconify-icon icon="boxicons:check-circle" width="44" class="text-white"></iconify-icon>
              </div>
            </div>
            <div class="p-4">
              <h3 class="font-bold text-sm text-white leading-snug"><%= c.title %></h3>
              <span class="inline-block mt-1.5 text-xs px-2 py-0.5 bg-charm-600/15 text-charm-400 rounded-md"><%= c.theme %></span>
              <p class="text-xs text-white/35 mt-2 flex items-center gap-3">
                <span><%= c.daysLeft %> days left</span>
                <span><%= c.submissionCount %> entries</span>
              </p>
            </div>
          </div>
          <% }) %>
        </div>

        <!-- Entry form -->
        <div id="entry-form" class="hidden mt-8 bg-shark-900 border border-white/8 rounded-md p-6">
          <h3 class="font-semibold text-base mb-5">
            Your entry for <span id="selected-contest-name" class="text-charm-400"></span>
          </h3>

          <!-- Photo upload zone -->
          <div class="flex flex-col gap-1.5 mb-5">
            <label class="text-xs font-semibold text-white/50 uppercase tracking-wide">
              Apron Photo <span class="text-charm-500">*</span>
            </label>
            <label id="entry-upload-zone" class="cursor-pointer block">
              <div id="entry-placeholder"
                class="border-2 border-dashed border-white/12 rounded-md p-10 flex flex-col items-center gap-3 text-white/35 hover:border-charm-600/40 hover:text-white/55 transition-colors">
                <iconify-icon icon="boxicons:image-add" width="40"></iconify-icon>
                <div class="text-center">
                  <p class="text-sm font-medium">Click to upload or drag & drop</p>
                  <p class="text-xs mt-0.5 text-white/25">PNG, JPG, WEBP — max 10 MB</p>
                </div>
              </div>
              <div id="entry-preview-container" class="hidden relative rounded-md overflow-hidden">
                <img id="entry-preview-img" src="" alt="Entry preview" class="w-full max-h-80 object-cover rounded-md">
                <div class="absolute top-2 right-2 bg-shark-950/80 rounded-md px-2 py-1 text-xs text-white/60 flex items-center gap-1 pointer-events-none">
                  <iconify-icon icon="boxicons:pencil" width="11"></iconify-icon>
                  Change
                </div>
              </div>
              <input type="file" name="entryPhoto" id="entry-input" accept="image/*" class="hidden">
            </label>
            <p id="entry-error" class="text-xs text-red-400 hidden"></p>
          </div>

          <!-- Entry title -->
          <div class="flex flex-col gap-1.5 mb-5">
            <label for="entry-title" class="text-xs font-semibold text-white/50 uppercase tracking-wide">
              Entry Title <span class="text-charm-500">*</span>
            </label>
            <input id="entry-title" name="entryTitle" type="text" placeholder="Give your apron a name"
              class="w-full bg-shark-800 border border-white/10 rounded-md px-3 py-2.5 text-sm text-white placeholder:text-white/20 focus:outline-none focus:border-charm-600/50 focus:ring-1 focus:ring-charm-600/25 transition-colors" />
            <p id="entry-title-error" class="text-xs text-red-400 hidden"></p>
          </div>

          <!-- Description -->
          <div class="flex flex-col gap-1.5 mb-6">
            <label for="entry-description" class="text-xs font-semibold text-white/50 uppercase tracking-wide">
              Description <span class="text-white/25 font-normal normal-case">(optional)</span>
            </label>
            <textarea id="entry-description" name="entryDescription" rows="3"
              placeholder="Describe your design, materials, inspiration..."
              class="w-full bg-shark-800 border border-white/10 rounded-md px-3 py-2.5 text-sm text-white placeholder:text-white/20 focus:outline-none focus:border-charm-600/50 focus:ring-1 focus:ring-charm-600/25 transition-colors resize-none"></textarea>
          </div>

          <% if (typeof error !== 'undefined' && error) { %>
          <p class="text-sm text-red-400 mb-4 flex items-center gap-1.5">
            <iconify-icon icon="boxicons:error-circle" width="15"></iconify-icon>
            <%= error %>
          </p>
          <% } %>

          <button type="submit" id="submit-btn"
            class="w-full bg-charm-600 hover:bg-charm-500 disabled:opacity-50 disabled:cursor-not-allowed text-white font-bold rounded-md py-3 text-sm transition-colors">
            Complete Sign Up
          </button>
        </div>

      </div>

    </form>
  </div>

<script>
(function () {

  // ── Utilities ──────────────────────────────────────────────
  function showErr(id, msg) {
    var el = document.getElementById(id);
    if (!el) return;
    el.textContent = msg;
    el.classList.remove('hidden');
  }
  function clearErr(id) {
    var el = document.getElementById(id);
    if (!el) return;
    el.textContent = '';
    el.classList.add('hidden');
  }
  function clearStep1Errors() {
    ['username-error','email-error','confirm-password-error','sex-error','birthdate-error','step1-general-error']
      .forEach(clearErr);
  }

  // ── Step navigation ────────────────────────────────────────
  function showStep(n) {
    var s1 = document.getElementById('step-1-content');
    var s2 = document.getElementById('step-2-content');
    var n1 = document.getElementById('step-1-num');
    var l1 = document.getElementById('step-1-label');
    var n2 = document.getElementById('step-2-num');
    var l2 = document.getElementById('step-2-label');

    var activeNum   = 'bg-charm-600 text-white';
    var inactiveNum = 'bg-shark-700 text-white/35';

    if (n === 1) {
      s1.classList.remove('hidden');
      s2.classList.add('hidden');
      n1.className = n1.className.replace(inactiveNum, activeNum);
      n2.className = n2.className.replace(activeNum, inactiveNum);
      l1.className = l1.className.replace('text-white/30', 'text-white/60');
      l2.className = l2.className.replace('text-white/60', 'text-white/30');
    } else {
      s1.classList.add('hidden');
      s2.classList.remove('hidden');
      n2.className = n2.className.replace(inactiveNum, activeNum);
      n1.className = n1.className.replace(activeNum, inactiveNum);
      l2.className = l2.className.replace('text-white/30', 'text-white/60');
      l1.className = l1.className.replace('text-white/60', 'text-white/30');
      window.scrollTo({ top: 0, behavior: 'smooth' });
    }
  }

  document.getElementById('back-btn').addEventListener('click', function () { showStep(1); });

  // If server returned an error, jump straight to step 2 + show entry form
  <% if (typeof error !== 'undefined' && error) { %>
  showStep(2);
  document.getElementById('entry-form').classList.remove('hidden');
  <% } %>

  // ── Avatar preview ─────────────────────────────────────────
  document.getElementById('avatar-input').addEventListener('change', function () {
    var file = this.files[0];
    if (!file) return;
    var reader = new FileReader();
    reader.onload = function (e) {
      var circle = document.getElementById('avatar-circle');
      circle.style.backgroundImage = 'url(' + e.target.result + ')';
      document.getElementById('avatar-placeholder').classList.add('hidden');
    };
    reader.readAsDataURL(file);
  });

  // ── Password show/hide ─────────────────────────────────────
  document.getElementById('toggle-password').addEventListener('click', function () {
    var pw   = document.getElementById('password');
    var icon = document.getElementById('pw-eye-icon');
    if (pw.type === 'password') {
      pw.type = 'text';
      icon.setAttribute('icon', 'boxicons:hide');
    } else {
      pw.type = 'password';
      icon.setAttribute('icon', 'boxicons:show');
    }
  });

  // ── Sex select placeholder color ───────────────────────────
  document.getElementById('sex').addEventListener('change', function () {
    this.style.color = this.value ? 'white' : 'rgba(255,255,255,0.22)';
  });

  // ── Username availability (debounced) ──────────────────────
  var usernameTimer = null;
  document.getElementById('username').addEventListener('input', function () {
    clearTimeout(usernameTimer);
    clearErr('username-error');
    var val = this.value.trim().toLowerCase();
    var statusEl = document.getElementById('username-status');
    statusEl.classList.add('hidden');

    if (val.length < 3) return;

    if (!/^[a-z0-9_]{3,20}$/.test(val)) {
      statusEl.innerHTML = '<iconify-icon icon="boxicons:x-circle" width="15" style="color:#f87171;display:block"></iconify-icon>';
      statusEl.classList.remove('hidden');
      return;
    }

    statusEl.innerHTML = '<iconify-icon icon="boxicons:loader-alt" width="15" style="color:rgba(255,255,255,0.3);display:block"></iconify-icon>';
    statusEl.classList.remove('hidden');

    usernameTimer = setTimeout(function () {
      fetch('/api/check-signup?username=' + encodeURIComponent(val))
        .then(function (r) { return r.json(); })
        .then(function (data) {
          if (data.usernameAvailable) {
            statusEl.innerHTML = '<iconify-icon icon="boxicons:check-circle" width="15" style="color:#4ade80;display:block"></iconify-icon>';
          } else {
            statusEl.innerHTML = '<iconify-icon icon="boxicons:x-circle" width="15" style="color:#f87171;display:block"></iconify-icon>';
            showErr('username-error', 'Username already taken');
          }
        })
        .catch(function () { statusEl.classList.add('hidden'); });
    }, 450);
  });

  // ── Step 1 validation ──────────────────────────────────────
  function validateStep1() {
    clearStep1Errors();
    var valid    = true;
    var username = document.getElementById('username').value.trim();
    var email    = document.getElementById('email').value.trim();
    var password = document.getElementById('password').value;
    var confirm  = document.getElementById('confirm-password').value;
    var sex      = document.getElementById('sex').value;
    var birthdate = document.getElementById('birthdate').value;

    if (!username) {
      showErr('username-error', 'Username is required'); valid = false;
    } else if (!/^[a-z0-9_]{3,20}$/.test(username.toLowerCase())) {
      showErr('username-error', 'Only letters, numbers, underscores (3–20 chars)'); valid = false;
    }

    if (!email || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
      showErr('email-error', 'A valid email is required'); valid = false;
    }

    if (!password || password.length < 8) {
      showErr('confirm-password-error', 'Password must be at least 8 characters'); valid = false;
    } else if (password !== confirm) {
      showErr('confirm-password-error', 'Passwords do not match'); valid = false;
    }

    if (!sex) {
      showErr('sex-error', 'Please select your sex'); valid = false;
    }

    if (!birthdate) {
      showErr('birthdate-error', 'Date of birth is required'); valid = false;
    } else {
      var age = Math.floor((Date.now() - new Date(birthdate)) / (365.25 * 24 * 60 * 60 * 1000));
      if (age < 13) { showErr('birthdate-error', 'You must be at least 13 years old'); valid = false; }
    }

    return valid;
  }

  // ── Continue button ────────────────────────────────────────
  document.getElementById('next-btn').addEventListener('click', function () {
    if (!validateStep1()) return;

    var btn = document.getElementById('next-btn');
    btn.disabled = true;
    btn.textContent = 'Checking…';

    var username = document.getElementById('username').value.trim().toLowerCase();
    var email    = document.getElementById('email').value.trim().toLowerCase();

    fetch('/api/check-signup?username=' + encodeURIComponent(username) + '&email=' + encodeURIComponent(email))
      .then(function (r) { return r.json(); })
      .then(function (data) {
        if (!data.usernameAvailable) {
          showErr('username-error', 'Username already taken');
          showStep(1);
        } else if (!data.emailAvailable) {
          showErr('email-error', 'Email already registered');
          showStep(1);
        } else {
          showStep(2);
        }
      })
      .catch(function () { showStep(2); })
      .finally(function () {
        btn.disabled = false;
        btn.innerHTML = 'Continue <iconify-icon icon="boxicons:right-arrow-alt" width="20"></iconify-icon>';
      });
  });

  // ── Contest selection ──────────────────────────────────────
  document.querySelectorAll('.contest-card').forEach(function (card) {
    card.addEventListener('click', function () {
      document.querySelectorAll('.contest-card').forEach(function (c) {
        c.classList.remove('ring-2', 'ring-charm-600', 'border-charm-600');
        c.querySelector('.contest-check-overlay').classList.replace('opacity-100', 'opacity-0');
      });
      card.classList.add('ring-2', 'ring-charm-600', 'border-charm-600');
      card.querySelector('.contest-check-overlay').classList.replace('opacity-0', 'opacity-100');

      document.getElementById('contestId').value = card.dataset.contestId;
      document.getElementById('selected-contest-name').textContent = card.dataset.contestTitle;

      var entryForm = document.getElementById('entry-form');
      entryForm.classList.remove('hidden');
      setTimeout(function () {
        entryForm.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
      }, 80);
    });
  });

  // ── Entry photo preview ────────────────────────────────────
  function showEntryPreview(file) {
    var reader = new FileReader();
    reader.onload = function (e) {
      document.getElementById('entry-preview-img').src = e.target.result;
      document.getElementById('entry-placeholder').classList.add('hidden');
      document.getElementById('entry-preview-container').classList.remove('hidden');
    };
    reader.readAsDataURL(file);
  }

  document.getElementById('entry-input').addEventListener('change', function () {
    if (this.files[0]) showEntryPreview(this.files[0]);
  });

  var zone = document.getElementById('entry-upload-zone');
  zone.addEventListener('dragover', function (e) {
    e.preventDefault();
    var ph = document.getElementById('entry-placeholder');
    if (ph) ph.classList.add('border-charm-600/60');
  });
  zone.addEventListener('dragleave', function () {
    var ph = document.getElementById('entry-placeholder');
    if (ph) ph.classList.remove('border-charm-600/60');
  });
  zone.addEventListener('drop', function (e) {
    e.preventDefault();
    var ph = document.getElementById('entry-placeholder');
    if (ph) ph.classList.remove('border-charm-600/60');
    var file = e.dataTransfer.files[0];
    if (file && file.type.startsWith('image/')) {
      var dt = new DataTransfer();
      dt.items.add(file);
      document.getElementById('entry-input').files = dt.files;
      showEntryPreview(file);
    }
  });

  // ── Form submit validation ─────────────────────────────────
  document.getElementById('signup-form').addEventListener('submit', function (e) {
    clearErr('entry-error');
    clearErr('entry-title-error');

    var contestId  = document.getElementById('contestId').value;
    var entryInput = document.getElementById('entry-input');
    var entryTitle = document.getElementById('entry-title').value.trim();
    var valid = true;

    if (!contestId) {
      e.preventDefault();
      showErr('entry-error', 'Please select a contest above');
      valid = false;
    }
    if (!entryInput.files || !entryInput.files[0]) {
      e.preventDefault();
      showErr('entry-error', 'Please upload your apron photo');
      valid = false;
    }
    if (!entryTitle) {
      e.preventDefault();
      showErr('entry-title-error', 'Entry title is required');
      valid = false;
    }

    if (valid) {
      var btn = document.getElementById('submit-btn');
      btn.disabled = true;
      btn.textContent = 'Creating your account…';
    }
  });

}());
</script>

</body>
</html>
